import React from 'react';
import './Hero.css';

export default function Hero(props) {
    return (
        <div class="hero">
            <div class="hero__text">MANAGE CAMPAIGNS</div>
        </div>
    );
};